# Evolução do projeto

Histórico completo: cada plateau de score atingido, cada tentativa descartada, e os
findings algorítmicos que custaram tempo. O `CLAUDE.md` aponta pra cá pra contexto
profundo; este arquivo é o registro auditável da pesquisa.

## Mudanças que ficaram (plateaus)

| # | Score | Técnica | Δ | Comprovação |
|---|---:|---|---:|---|
| 0 | −6000 | numpy brute-force partitioned (M1-M5 PRD original) | — | k6 oficial: ~15ms/query satura 1 CPU @ 900 RPS → fila → 80% timeout |
| 1 | 738 | Faiss `IndexIVFFlat` + `mmap` (IO_FLAG_MMAP) por partição, `nprobe=8` | **+6738** | prévia oficial (M6) |
| 2 | 1528 | `nprobe 8 → 1` (baked em `build_index`) | **+790** | prévia: cada lista IVF a mais era latência pura sob saturação |
| 3 | 1697 | fail-safe `try/except` no `fraud_score` handler, default = fraud | **+169** | prévia: crashes em payloads edge eliminados (http_errors pesa 5× no E) |
| 4 | 2512 | bbox `lb_sq` cross-partition + unanime-exit + cap=8 | **+815** | prévia: `min`/`max` por dim em `meta.json`; pula sweep se 5/5 mesma label |
| 5 | 2921 | `nprobe 1 → 2` (baked) | **+409** | prévia: com bbox+unanime, dá pra cavar mais fundo na primária |
| 6 | 3045 | asymmetric `EXTRAS_NPROBE=3` (primary fica em 2) | **+124** | prévia: extras só rodam após bbox filter, vale recall maior |
| 7 | **3190-3233** | fast-path determinístico ANTES do KNN (2 regras hand-derivadas) | **+145-188** | prévias #7218/#7232. `amount/avg ≤ 0.971` → legit; `amount > 2996` → fraud. 92.6% cov 99.99% pureza |
| 8 | **3236** | `IndexIVFScalarQuantizer` fp16 storage | 0, mem -43% | prévia #7242. Mesma banda (3190-3233) mas index 147→84 MB, working set menor — folga pro tail sob pressão. Faiss decomprime pra float32 pra SIMD distance |
| 9 | **3279** | RSGI nativo (drop Starlette/ASGI) + `PYTHONOPTIMIZE=2` + `--http 1 --no-ws` | **+43** | prévia #7292 no `1bc7158`. p99 65→57 ms (-12%). Detection neutra (FP+1). Diff: -279/+24 LOC, removeu starlette dep. Body da issue precisa ser `rinha/test smarzaro-python` (não só o título — runner parsa o body) |
| 10 | **3691** | Single global IVF (nlist=2048, nprobe=12) + drop bbox sweep + Python partition_key (cleanup) | **+412** | prévia #7450 no `d4af075`. Detection 2038→2480 (FP 64→32 -50%, FN 18→7 -60%, failure_rate 0.15%→0.07% abaixo do floor). `rate_component=3000` no CAP máximo (error rate < MIN_EPSILON 0.001). **K-means coarse quantizer agrupa por L2 real** vs hand-crafted partition_key arbitrária — não precisa mais do bbox sweep pra compensar desalinhamento. 1 faiss.search/query (vs 1-9 antes). Profile revelou throttle externo do cgroup ser o gargalo k6 — isso libera CPU budget pra recall melhor (faiss_search avg 469→963µs interno mas k6 score subiu). Sim projetou +155, Haswell real entregou +412 (mais CPU = throttle menos severo). Diff -89 LOC (removeu bbox, fallback partition índices, extras_nprobe, MAX_EXTRA_PARTITIONS) |
| 11 | **3722** | K-means training `cp.niter=25 cp.nredo=4` (vs default 10/1) | **+31** | prévia #7619 no `2b0559e`. Detection 2480→2490 (FN 7→5 -29%, FP 32→34 +6%, net E 53→49 -7.5%). p99 63.7→58.5 ms (-8%). Sim avg 3707→3705 (dentro de ruído de 12 pts) mas detection determinística — 3 réplicas idênticas FP=34 FN=5. Real Haswell carregou a melhoria de detecção exatamente como previsto + ganho de p99 não modelado pelo sim. Custo: +8s no build, runtime 0 |
| 12 | **4091** | IVFFlat fp32 (drop SQ_fp16) + HAProxy CPU 0.10→0.20 (APIs 0.45→0.40 cada) | **+368** | prévia #7825 no `3bc922a`. Diagnose com `taskset -c 0` revelou que IndexIVFScalarQuantizer fp16 leva 798µs/query vs IVFFlat fp32 102µs (7.8×) — em 14-dim a decompressão fp16 domina. HAProxy a 0.10 estava CPU-starved → 1-5 ERR por sim run; a 0.20 ERR=0 sistemático. p99 58.5→25.7 ms (−56%); detection 2490→2501 (FP 34→33, FN 5→4). Index 84→184MB; runtime resident set 87/165MB no sim (mmap evict de cold pages mantém working set baixo). 5 sim replicates 3766-3802 (band 36 pts, ERR=0 todas), detection idêntica FP=33 FN=4 E=45 — sim previu real com precisão excepcional. Real Haswell entregou p99 ainda melhor que sim modelou (25.7ms vs sim 53ms) por ter mais CPU absoluto |
| 13 | **4177** | Pure numpy IVF (drop Faiss do runtime — só treina k-means no build) | **+86** | prévia #7906 no `e59c6e1`. Substrato totalmente em numpy: centroides + cluster-sorted vectors + norms pré-computadas; distância via norm-expansion (`||a-b||² = ||a||² + ||b||² - 2·a·b`), elimina alloc de `(block - q)` e crossing Faiss↔Python por query. Internal search_avg 957µs Faiss → 443µs numpy no sim (-54%); total p99 interno 1633→1205µs. Detection IDÊNTICA ao plateau #12 (FP=33 FN=4 E=45) — mesmas centroides + mesmo nprobe = mesmo recall. p99 real 25.7→21.1ms (-18%). **Hipótese da Lever 5 (numpy híbrido com Faiss centroid) confirmada às avessas**: aquele fracasso era do MIXING das duas engines sob CPU regimes diferentes; single-engine numpy reproduz sim→real direção. 5 sim replicates 3805-3844, identical detection. Levers já testadas em cima do Faiss (`Adaptive nprobe`, `Cell-mean`, `Tree pre-classifier`) precisam ser re-avaliadas no novo motor — comportamento sob numpy puro pode ser diferente |
| 14 | **4275** | Granian `--loop uvloop --http1-pipeline-flush` | **+98** | prévia #8221 no `8cb9a63`. Adicionou uvloop como dep (pip pkg), trocou event loop padrão asyncio por uvloop. Detection IDÊNTICA (FP=33 FN=4) — só I/O scheduling. Sim mostrou +40 (3866 vs 3826 baseline, p99 47→43ms), real entregou +98 (p99 21.1→16.83ms, p99_score 1647→1774). Sim subvalorizou porque sim CPU-bound (0.16) já estava com gargalo em compute throttle; em real (0.40, folgado) o tail externo de body_read async era o BOTTLENECK do gap k6↔internal — uvloop cortou 40% do body_read tail (1193µs→711µs em sim) que destravou p99 em cascata. **Lição inversa de numba**: levers que reduzem COMPUTE são supervalorizados em sim e regridem em real (numba +24 sim, -29 real); levers que reduzem I/O SCHEDULING são subvalorizados em sim e dão mais em real |
| 15 | **4493** | Granian `--workers 2` per container (4 worker processes totais via fork; mmap COW-shared) | **+218** | prévia #8233 no `7d9a7ac`. Cada container vira 2 processos Granian que herdam mmap do index via COW de fork — memória só +11 MB per process extra (89 MB / 165 budget). 4 workers totais (2 containers × 2 workers) processam ~225 RPS cada (vs 450 RPS antes) → menos head-of-line blocking + IVF queue por worker mais curta. Sim previa +24 (3890 vs 3866), real entregou +218 (p99 16.83→10.19ms, p99_score 1774→1992). Detection IDÊNTICA. Mesma assinatura de uvloop: lever I/O scheduling subvalorizada em sim. **Insight de pareamento**: workers=2 falhou antes (asyncio loop) — só funciona com uvloop. Asyncio com 2 workers tinha contenção do GIL no event loop principal. uvloop libera Python para fora do event loop |
| 16 | **4593** | PyO3+Rust crate `knn_simd` com AVX2 `VPMADDWD` em int16 quantizado | **+100** | prévia #8329 no `748a93e`. **Cruza a linha "Python idiomático sem extensões nativas"** — escreve Rust autoral compilado num `.so` via PyO3+maturin. Build pipeline: refs quantizadas a int16 (scale=10000, lossless porque refs são round4), padded de 14 → 16 lanes por vetor (1 AVX2 register de 256-bit). Kernel: `_mm256_sub_epi16` + `_mm256_madd_epi16` (16 i16 muls + 8 i32 adds por instrução), top-5 mantido em Rust via insertion-sort. Single PyO3 call por query (vs uma per-cluster antes). Compilado com `RUSTFLAGS=-C target-cpu=haswell` no Dockerfile stage. Profile interno: IVF avg 415µs → **107µs (-74%)**, IVF p99 637µs → **160µs (-75%)**. Memória: 90 MB / 165 (sem overhead — .so é ~1MB, mmap compartilhado). 5 sim runs avg 3909 vs w2 3890 = +19 (upper noise band). Real entregou +100 (p99 10.19→8.10ms, p99_score 1992→2092). Detection IDÊNTICA (FP=33 FN=4 ERR=0). Sim subvalorizou 5× — mesma assinatura de uvloop/workers=2. **Por que funcionou onde numba falhou**: (1) AOT compiled, sem JIT race; (2) zero memory overhead (vs numba +25 MB/process); (3) single PyO3 call total, sem overhead per-cluster; (4) shared mmap entre os 2 worker processes do container. **Padrão consolidado**: levers que reduzem compute variance em real CPU-folgado dão +100-200 quando não introduzem overhead estrutural. Sim CPU-bound (0.16) supervaloriza compute saves; real precisa do lever ser efficient too (não só rápido em microbench) |
| 17 | **4805** | NPROBE 12 → 32 com Rust SIMD (Rust faz recall mais alto barato) | **+212** | prévia #8350 no `2b17930`. Com Rust SIMD o custo por extra cluster ficou ~5µs em vez de ~30µs (BLAS). Sim mostrou +33 (3942 vs 3909), real entregou +212. Detection sobe de 2501→2530 (FP 33→30, FN 4→2 — 5 boundary errors fechados, E 45→36). **p99 também caiu** 8.10→5.30ms (-2.8ms): mais recall = menos variance no veredito = menos chance de queue. p99_score 2092→2275 (+183). Combined det+p99: +212 vs predição sim +33 = subvalorização de 6×. **Lição**: com Rust SIMD, antes-bloqueadas combinações (re-probe, nprobe alto) viram baratas. O profile sim mostrava IVF avg 107µs → 165µs (+58µs); aparentemente isso destrava cascata de I/O wins em real |
| 18 | **4957** | A) `abs()` em minutes_between_last_tx (paridade exata com refs); B) vectorize+partition_key em Rust PyO3 byte-direct ISO (sem datetime, sem numpy alloc); **C) drop IVF, KD-tree exato com branch+bound + AVX2 `_mm256_max_epi16`/`_mm256_min_epi16` para lb² de bbox** | **+152** | prévia #8441 no `92c3471`. **Refutação direta**: experimento nprobe=64+bbox (#8409) atingiu det 2557 com IVF aproximado mas pagou p99; sinal claro de que o caminho era exact KNN, não mais recall via IVF. Build time: KD-tree leaf=64 sobre 3M vetores int16, 131K nodes (10.5 MB), 65K leaves (~46 vec/leaf). Runtime: iterative DFS com prune `lb² ≥ top5.worst()`, AVX2 leaf scan reaproveita `dist_avx2_pair`. Sim 3 runs 3957/3955/3948 = avg 3953 (+11 vs B). Real: det 2502→2557 (FP 30→29, FN 2→0 — finalmente fechou os FN deterministicamente; FP=29 é o floor empírico observado em múltiplas implementações com KNN exato sobre as mesmas refs). p99 5.30→3.97ms (-1.33ms) porque exact KNN visita ~centenas de vetores em vez de 32×225=7200. p99_score 2275→2401 (+126). **Sim previu +11, real +152** — assinatura de precisão+I/O amplificada em CPU folgado. Memória 89→100 MB (kd-tree 10MB + vectors_kd duplicado mas com paginação efetiva via mmap). **Lição**: sub-valorização de sim 14× quando precisão+latência caminham juntas. IVF approx era o teto invisível |
| Tentativa | Resultado | Por quê |
| NPROBE 32 → 64 (det floor ético) — prévia REAL #8367 (commit `9523351`) | **−353 real** (4805 → 4452) | nprobe=64 fecha o último FN (FP=29 FN=0 = det floor 2557, +27). Sim 3955 (+13 vs nprobe=32). MAS real p99 explodiu 5.30→12.74ms (+7.4ms), p99_score 2275→1895 (-380). Det gain +27 não pagou pelo p99 loss -380. **Diagnóstico**: 4× mais clusters scaneados na real Haswell (memory bandwidth do int16 mmap) saturou cache → cluster scan ficou 3× mais lento que sim previa. nprobe=32 é o sweet spot real onde det 2530 + p99 5.30ms = 4805. Diminishing returns severos depois |
| Cluster bbox prune + NPROBE 64 (Rust AVX2 `_mm256_subs_epi16` saturating + `_mm256_max_epi16` → `_mm256_madd_epi16` para lb²) — prévia REAL #8409 (commit `051ec0b`) | **−176 real** (4805 → 4629) | Bbox prune corta clusters cujo lower-bound² ≥ atual worst-of-top-5: sim mostrava IVF avg **90µs** (-68% vs np=64 plain 281µs; até MENOR que plateau #17 nprobe=32 plain 165µs). 3 sim runs avg 3959 (+17 vs np=32, det floor FP=29 FN=0 idêntico ao np=64 puro). Real: detection ganhou +55 (2502→2557, FN closes) MAS p99 5.30→8.46ms (+3.16ms), p99_score 2275→2072 (-203). Net −176. **Diagnóstico**: bbox reduz o AVG (clusters pruneados early) mas o WORST-CASE (clusters densos onde lb² < worst-of-top5) ainda paga 4× scan + overhead da prova de bbox por cluster. Sim 0.16 CPU throttle uniformiza tail; real Haswell amplifica a variance entre clusters pruneados vs cheios. **Padrão consolidado**: levers que melhoram AVG mas alargam tail variance regridem em real, mesmo com detection no floor ético. nprobe=32 plano segue intocado como sweet spot |
| Per-partition KD-trees + primary-first cross-partition prune (256 sub-trees, bbox-sorted cross-partition) — prévia REAL #8461 (commit `e0541e2`) | **−7 real** (4957 → 4950, dentro do ruído ±15) | Estrutura: build_index quebra refs por `partition_key` 8-bit, constrói KD-tree por bucket (102 não-vazios), concatena em arrays globais com `partition_roots[256]` + `partition_bbox_{min,max}[256, 16]`. Runtime: walk primary tree primeiro (lb=0), depois itera 255 outras partições por bbox-lb ordenado com prune. Sim 3 runs 3955/3972/3955 = avg 3960. Internal kernel ganhou massivamente: knn_simd p99 517→140µs (-73%), total p99 442→231µs (-48%). **Mas real essencialmente neutro**: gargalo do plateau #18 não era mais o KNN (já era 182µs avg/boundary ≈ 18µs/req médio) — era o tail externo do RSGI body_read (~200-400µs p99). Otimizar o KNN abaixo de "não é mais o gargalo" é invisível ao k6. Mantido como base estrutural pra próximas alavancas (early-exit, sub-tree variants); sem regressão de detection (FP=29 FN=0 idem) |
| Cross-partition early-exit (threshold = 2_000_000, sweet spot do sim sweep onde det fica em FP=29 FN=0) — prévia REAL #8479 (commit `c99318b`) | **−57 real** (4957 → 4900) | Após walk do primary, se `top5.worst() < EARLY_LIMIT` ⇒ retorna sem cross-partition. Sim sweep: ≤2M det idêntico (FP=29 FN=0), >5M det desaba (primary perde true 5-NN). Sim 3 runs 3961/3938/3974 = avg 3958 (idêntico a D sem early-exit). Real: p99 3.97→4.53ms (+0.56ms), p99_score 2401→2344 (-57). Det idem 2557. **Mesma assinatura do bbox prune (#8409 −176)**: early-exit cria DUAS classes de queries (easy = só primary ≈ 50µs, hard = primary + cross-partition ≈ 200µs) e a variance widening custa p99 mais do que o average win economiza. Sim com 0.16 CPU throttle uniformiza tail; real Haswell amplifica a variance. **Padrão consolidado #2**: condicionais que criam paths de custo diferente regridem real, mesmo com detection no floor. Mantido em código mas `EARLY_LIMIT` default = 0 (disabled). **NOTA**: bug encontrado posteriormente — o "default disabled" usava `i64::MAX` como sentinela, mas a condição `top5.worst() < limit` SEMPRE disparava nesse caso. Corrigido em `8275b51` (sentinela = 0). Significa que a prévia #8461 atribuída a "D pure" também estava testando primary-only KNN bugado; D-fixed nunca foi medido isolado em real |
| 3rd-rule fraud cascade (`tx_count_24h >= 16`, `km_from_home >= 700`, `last.km_from_current >= 695`) — prévia REAL #8627 (commit `fcf4503`) | **−1 real** (4957 → 4956, ruído puro) | Mineração sobre 223k boundary refs achou 4 regras com 100% pureza individualmente. Cross-validação contra `bench/test-data.json` derrubou `installments >= 10` (1 legit em bench numa tx max-installments gambling-MCC) e manteve as outras 3 com 0 FP em bench também. Sim 3 runs 4000/3957/3978 = avg 3978 (idem D-fixed 3981). Path mix mudou: fast_fraud 37.5% → 40.9%, boundary 10% → 6% (KNN load −40%). Profile interno: total avg 142→120µs, p99 231→191µs. **Real: zero impacto**. Confirma a tese final: desde plateau #18 (KD-tree exato) o KNN deixou de ser o gargalo do p99 real (que é dominado pelo tail externo do RSGI body_read 200-400µs). Cascade mantida porque: (1) zero FP adicional, (2) zero p99 cost, (3) código mais limpo + 40% menos trabalho de KNN. Detection idem 2557 deterministic |

## Tentativas em cima do single-IVF (todas descartadas)

| Tentativa | Resultado | Por quê |
|---|---:|---|
| Per-cluster purity skip (`quantizer.search(q,1)` + cluster_majority lookup; 1825/2048 clusters puros @ 99.9%, 1786 @ 1.0) | -57 sim | 62.5% das boundary cortaram faiss (478→963µs cut), MAS detection regrediu (FP +5, FN +1). Pure cluster ≠ correct K-NN: a true query pode estar mais perto de neighbors em OUTRO cluster que o IVF sweep encontraria. **Lossy fundamental** |
| nlist tuning (1024, 4096) | 0 sim | (2048, nprobe=12) já saturada. (4096, 24) mesmo total scan: tied. (1024, 6) coarser: tied/pior |
| `--runtime-mode mt --runtime-threads 2` no single-IVF | -7 sim, p99 +6ms | Threads overhead > paralelismo. GIL + faiss interno C++ não compõem bem |
| `MAP_POPULATE` no `global.faiss` (single mmap contígua) | 0 sim | Page cache aquece organicamente nos 2min de ramp do k6 |
| vectorize internals (manual ISO parse) | -217 ns/req | microbench Py 3.14: `fromisoformat` 416ns < slice manual 633ns. Datetime tem C-impl otimizado, manual python perde. Mesmo se ganhasse, seria 0.05µs avg agregado em 10% boundary |
| Pre-allocated `np.empty(14)` buffer (singleton st-mode) | 0 sim (3709 vs banda 3701-3713) | Alloc custa ~272ns/call mas em 10% boundary = 0.03µs avg agregado, dentro de ruído estatístico |
| nprobe sweep fino (8/10/14 vs 12) com 2 réplicas cada | 0 sim | nprobe=10 +13 avg (banda 30 pts run-to-run), nprobe=14 melhor detection (FN=5) mas p99 +7ms cancela. Pico ainda em 10-12 |
| CPU pinning (`cpuset: '0'`/`'1'` por container) | 0 sim | Sim host tem cores sobrando (sem contenção L1/L2 pra reduzir); real Rinha é single-core onde cpuset é no-op anyway |
| Cluster homogeneous skip @ 1.0 floor (replicate com niter=25) | descartado por prior data | 1784/2048 clusters pure @ 1.0 com niter=25 (vs 1786 com niter=10) — k-means mais firme não muda estrutura de purity. Ceiling estrutural: boundary query pode ter true K-NN em cluster vizinho mesmo com cluster próprio puro |
| Decision tree pre-classifier (depth=10, min_leaf=50, sample=500k, conf=0.95) APÓS as 2 regras | -100 a -150 sim | Tree treina em references e cobre 53% do boundary com 99.6% accuracy em test-data, mas sob k6 saturado FP saltou 34→43-49 e ERR variou 1-5 (vs baseline 0-3). Detection 2382→2257 avg. Faiss KNN sobre 5 vizinhos é estritamente melhor que tree no boundary porque o que sobrou após 2-rule é exatamente a fatia ambígua onde recall importa |
| Adaptive nprobe (LOW=6 primário, HIGH=16 quando fc∈{2,3}) | 0 sim, faiss avg +18% | Profile revelou que ~51% das queries boundary triggam HIGH pass — boundary é intrinsecamente ambíguo após 2-rule já cortar o fácil. avg faiss subiu 1082→1272µs em vez de cair. Variantes (LOW=4/8, HIGH=12, ambig={1,2,3,4}) todas pioraram. Lever errado pro nosso shape de boundary |
| Cell-mean fast-path sobre IVF cluster (margem 0.01) | -120 sim | Mesmo problema do tree: o 7.4% que sobra após 2-rule é o conjunto onde majority-vote do cluster diverge do KNN-exato. FN +5 estrutural (frauds que vivem em clusters predominantemente legit), FP +4. Detection 2382→2216 avg |
| Tree 100% (substitui as 2 regras): vectorize → tree → Faiss | 0-100 sim, FP=42-57 | Tree em todas as queries pega 95.4% (vs 92.6% do 2-rule), corta Faiss frequency 54% (2715→1231). MAS FP saltou 34→49-57 consistente (conf=0.95 ou 0.99). Tree é fundamentalmente menos preciso que 2-rule no espaço amount/avg + amount: 2-rule usa thresholds hand-derivados num espaço raw que generaliza melhor pro test que árvore treinada em references vetorizadas. Projeção real: det 2410-2360 vs 2490, -82 a -130 |
| Numpy IVF híbrido (Faiss só pro quantizer, cluster scan em numpy fp32 mmap) | **−74 real, +34 sim** | prévia #7873 no `524e322`: 4017 vs plateau #12 4091. Sim mostrou ganho ilusório (avg 3810 vs 3776, faiss_avg interno 957→731µs no sim), mas Haswell real entregou p99 30.5 vs 25.7ms (+5ms). Faiss IVFFlat tem AVX2 interno que se beneficia mais com quota CPU real (0.40) vs sim throttle (0.16). Bench bare-metal já dizia numpy seria mais lento (281µs vs 102µs); sim throttled inverteu por causa de overhead Python/Faiss diferente sob CPU starvation, mas real Haswell restaura a hierarquia. **Lição**: sim → real diverge quando muda o substrato algorítmico (não só parâmetros). Sim só prevê confiável dentro do mesmo motor. Revertido em `8fddef6` |
| `IndexIVFFlat` fp32 wide pre-plateau-12 (revert plateau 8 isoladamente, antes do HA bump) | 0 sim, +100MB | Mesma detection que fp16 (não-lossy a 14 dims) mas index 84→184MB. Sozinho não pagava — só quando combinado com HA 0.10→0.20 (plateau #12) virou win. **Levers compõem mesmo quando isolados parecem neutros** |
| Smart ε-dedup das refs (eps=0.05, 3M→1.27M, mesma-label balls collapsed) | **−36 real, +17 sim** | prévia #7921 no `1d4fe1d`: 4141 vs plateau #13 4177. Sim mostrou ganho marginal (avg 3850 vs 3833) com detection IMPROVED (FN 4→0, FP 33→41, E 45→41, det 2501→2513). Mas em real Haswell p99 PIOROU (21.1→23.6ms), gerando p99_score -48 que come o detection +12. Hipótese: dataset menor altera padrão de acesso mmap de forma que a working set fica mais fria; ou variância natural prévia-a-prévia (~10-15%). Random subsample 50% testado como controle: catastrófico (E saltou 45→1271), refs NÃO são globalmente redundantes — só locais em ε-balls puras. eps=0.02 (76% kept) e eps=0.10 (42% kept) ambos piores. Revertido em `e49b1ef` |
| Cell-mean restrito a clusters densos+puros (densidade ≥ p90 size, 100% pure, 161 elegíveis) | 0 sim | Sob numpy IVF, retestado o cell-mean fast-path desta vez com filtro duplo: cluster precisa ter size ≥ p90 E score 100% puro. Detection IDÊNTICA ao baseline (FP=33 FN=4 E=45) — fast-path só dispara em queries cujas KNN ficam dentro do cluster (porque é tão denso/homogêneo que não precisa cross-cluster). Mas score sim mesmo (3834 vs 3833): o fast-path economiza trabalho em queries que já eram fáceis, não nas slow do tail. Relaxar (pure_floor=0.02) regrediu detection (FN+8). Lever saturado: não tem trabalho a cortar nas queries que dominam p99 |
| HAProxy `nbthread 2` (com CPU 0.20) | +2 sim noise | Sim avg 3835 vs baseline 3833 — dentro de ruído. HAProxy a 0.20 CPU não está saturada, 2 threads não ajudam. Sem prévia |
| `gc.disable()` no startup do worker | **−27 real, +7 sim** | prévia #7932 no `456ad56`: 4150 vs plateau #13 4177. Sim mostrou variância apertada (5/5 acima 3836 vs baseline band 3805-3844) sugerindo eliminação de GC pauses, mas real Haswell deu p99 22.5 vs 21.1ms (+1.4). Hipótese: refcount imediato já bastava; gen-2 collection era irrelevante na prática. Variância preview-to-preview ~±30 pts; -27 está no limite mas a direção é clara. Revertido em `9785c8e` |
| Triangle-inequality stack: D (intra-cluster searchsorted cutoff) + B (cluster-radius skip) + C (early-stop probe via lower-bound ordering) | 0 sim em 9 runs | Pré-computou `vec_dvc[N]` (dist de cada vetor ao seu próprio centroide, sorted ASC within cluster) + `cluster_radii[nlist]` (max d_vc por cluster). Em runtime, probar clusters em ordem ASC de `max(0, d_qc − r_c)`, usar searchsorted em `vec_dvc[s:e]` por `[d_qc − √kth, d_qc + √kth]` pra cortar o scan, pular clusters inteiros com radius skip, abortar probe loop quando `lb_sq > kth_sq`. Pruning exato — detection IDÊNTICA em todas as variantes (FP=33 FN=4 ERR=0). 3 runs D-só (3818.66 avg), 3 runs D+B (3825.21), 3 runs D+B+C (3823.99) vs baseline #13 ~3833 (band 3805-3844). Todos dentro do ruído. **Diagnóstico**: o overhead Python do loop per-cluster (12 iterações × ~3-5µs each = ~50µs) absorve o ganho do pruning, porque o substrato atual (1 concat + 1 matmul vetorizado de 17k×14) já é tão denso em SIMD que cortar 50-70% do trabalho não compensa pagar Python frame setup × 12. Lever travado pelo regime: numpy vectorization premia menos partições maiores; iteração granular só venceria sob C/Rust. Sem prévia |
| `round4` fix em `vectorize.py` (np.round(v, 4) antes do return) | 0 sim em 3 runs | Data generator faz `round4` em refs (`refs[i].v[j] = round4(refs[i].v[j])`, linha 758) E em entries (`entries[i].vec[j] = round4(entries[i].vec[j])`, linha 797) antes de computar o oracle KNN. Nosso vectorize gerava fp32 com precisão completa; queries divergiam dos refs até ~5e-5/dim. Hipótese: isso fechava parte dos 37 errs (33 FP + 4 FN). **Refutada**: 3 runs com round4 mostram FP=33 FN=4 ERR=0 idênticos ao baseline, det=2501.17 exato. As 37 errors são **recall errors do IVF (nprobe=12)**, não erros de precisão. Sim mean 3845.84 vs baseline 3833 (+13, dentro do ruído). Fix mantido como correctness (custo ~50ns/query, casa com semântica do oracle). Sem prévia |
| Box-B subset curado (drop refs com 25/25 LOO mesma label, mantém só ~3.5% boundary) + retrain IVF nlist=256 sobre subset + nprobe=16 | **−2042 sim catastrófico** | Compute LOO k=25 via IVF temporária (nlist=2048, nprobe=64) sobre 3M refs, marca como "interior" refs cujos 25 vizinhos compartilham label, dropa interior. Subset = 105.517 refs (3.52%, casa exatamente com análise externa). Retreina centroides sobre subset (nlist=256, niter=25 nredo=4, p50=396/cluster bem balanceado). Index 186MB→9.5MB. **Falha**: sim 4177→2135, FP 33→330, FN 4→240. **Diagnóstico**: nosso 2-rule fast-path (92.6% cov, 99.99% purity) NÃO é um router suficiente — o 7.4% que cai no IVF inclui muitas queries interior-legit que o rule não pegou (ex: amount/avg=1.0 com amount<2996). Sem refs interior no IVF, essas viram FP em massa. Box-B só funciona com router preciso (um MLP 14→64→64→3 ou equivalente classifica 96.5%+ corretamente em A-legit/A-fraud/B-boundary; nosso 2-rule mira só "claramente legit/claramente fraud" e deixa muito ambíguo no resto). Revertido. **Lição**: a separação "interior vs boundary" no IVF só é segura quando o router upstream tem cobertura E precisão alta o suficiente pra GARANTIR que tudo que vai pro IVF é boundary genuíno |
| Two-stage borderline re-probe (fc∈{2,3} → NPROBE 12→{16,20,24,32}) | det +0 a +85 mas **p99 sim-catastrófico** | Quando o top-5 do scan inicial vota fc∈{2,3} (3-2 split, 1 vizinho de virar veredito), re-scan com nprobe maior. **Detection sobe genuinamente**: N=16 (+6), N=24 (+51, FP 33→21), N=32 (+85, FP 33→17, FN 4→2). Mas qualquer N≥20 satura a 0.16 CPU sim (k6 "Insufficient VUs reached 250"): p99 jump 21→307 (N=20), 21→1102 (N=24), 21→1596 (N=32). Sim score colapsa pra 2382-3013. **Trade-off impossível na sim**: NPROBE_REFINE alto suficiente pra ganhar det esgota CPU; baixo o bastante pra não esgotar não ganha det. Implementação incremental (só scanear próximos clusters e mergir top-5) deu NPROBE_REFINE=32 p99=233ms mas det só +21 (FP 33→29) — equivalência matemática quebra quando pass1 confidentemente erra (fc=5 confiável mas true top-5 inclui legits do pass2 não capturados). Hipótese: real 0.40 CPU (2.5× sim) talvez absorva o custo. Não burnado porque sim é confiável para CPU-bound levers e o risk-reward em prévia é ruim (-100 a +85 com viés negativo). Revertido. **Lição**: Levers que adicionam trabalho condicionalmente são premiados em real (mais CPU absoluto) mas castigados em sim (sub-limiar). Quando o gain depende de re-scan completo e a sim claramente esgota, evitar prévia |
| 23-bit lookup table (8M cells, 16 MiB) sobre 8 features [amount=6 bits, ratio=5, mcc=4, km_home=3, km_curr=2, online=1, present=1, unknown=1], aplicado APÓS 2-rule | **−30 a −33 sim** (det regrediu) | Build offline: histograma cada ref em uma célula de 23-bit signature; armazena `(legit_count<<8)\|fraud_count` clipped a uint8. 14841 cells não-vazias (0.18% do espaço), 12150 puras (82% das ocupadas, sugere coverage promissor). Runtime: 2-rule primeiro (intacto), se passar computa key via `bisect_right` nas bin edges + lookup direto. Decisão se cell `total≥MIN_SUPPORT AND fraud_count==0 OR legit_count==0`. Testado MIN_SUPPORT=5 (FP 33→40, FN 4→5, det 2501→2468) e MIN_SUPPORT=50 (FP 33→39, FN 4→5, det 2501→2477). **Falha estrutural**: "cell pura em refs" ≠ "true KNN over 3M é puro". Para queries no boundary, a vizinhança real spans múltiplas cells, então um query em cell pure-legit pode ter K-NN reais incluindo refs de cells vizinhas com fraud. Mesmo bug fundamental que matou cluster-purity skip (−57 sim, anos atrás). 8 queries-extra erradas vs 0 capturadas. **Diagnóstico mais profundo**: a alocação de bits foi pobre — amount (6 bits) e ratio (5 bits) já são exatamente as features usadas pelo 2-rule, então as bins refinadas dessas dims não ajudam no residual. As features que DECIDEM o residual são km_home, km_curr, hour, mcc — onde tínhamos bits insuficientes. **Lição**: lookup só funciona se as features são DESCORRELACIONADAS do que o router upstream usa. Reallocar bits pra features residual-discriminantes pode salvar mas não trivial sem batch de testes |
| L2 v2: 23-bit lookup com bits realocados pra km_home/km_curr/mcc/hour-heavy [5/5/5/4/1/1/1/2] | **−217 sim** (det 2501→2271) | Tentativa de fix do L2 v1: drop bits de amount/ratio, hour extraído via `tx.requested_at[11:13]`. Cell stats melhores: 83.6k non-empty (1% vs 0.18%), 91% puras (vs 82%). **Pior que v1**: cobertura maior = MAIS queries em pure cells = mais erradas. Confirma diagnóstico estrutural: cell-purity sobre subset de 8/14 dims NÃO prediz KNN over 14 dims pra boundary queries — é fundamentalmente lossy. Revertido. **Lição final**: lookup-table approach exige router upstream MUITO preciso (95%+ cov com 99.9%+ purity), nosso 2-rule não atende |
| Two-stage re-probe NPROBE=32 fc∈{2,3} — prévia REAL #8082 (commit `a2b67f1`) | **−1276 real** | Burnado pra testar hipótese "sim 0.16 CPU castiga, real 0.40 CPU absorve". REFUTADA. Real: p99 21→422ms (+401, sim previa +1575), det 2501→2526 (+25, sim previa +85), FP 33→28, FN 4→3, score 4177→2901. Sim CPU-saturation TAMBÉM se manifesta em real, só que mais gradualmente — 2.5× headroom não é suficiente pra workload onde 5% queries dobram o trabalho. p99_score colapsou de 1676→374 (efeito log do p99 22ms→422ms). Detection ganho foi real mas marginal demais. **Lição**: sim CPU-bound (k6 VUs saturados) PREDIZ real CPU-bound, não tem o "2.5× folga real" que eu hipotetizei. Levers que adicionam trabalho condicional precisam custar <2% de average, não 5-10% como esse. Revertido em `7b58fb2` |
| Product Quantization M=7 K=256 sub_dim=2 + fp32 rerank (K_RERANK=24/32/64) | **−75 a −337 sim** | Build offline: 7 sub-quantizers via Faiss k-means K=256, encode refs em 7 bytes (codes.npy = 21 MB vs vectors.npy 168 MB). Runtime: query → 7×256 LUT (einsum) → `lut[arange(7), codes].sum(axis=1)` → top K_RERANK por dist aproximada → rerank fp32 sobre esses K_RERANK candidatos → top-5. K_RERANK=64: det IDÊNTICA ao baseline (FP=33 FN=4 ERR=0) — rerank exato preserva recall — MAS p99 +8ms (54.9 vs 47ms) e score -75. K_RERANK=32: det perde -24, K_RERANK=24: perde -251 FN. **Diagnóstico**: BLAS fp32 matmul (`block @ query`, 17k×14) é **mais rápido** em numpy que PQ LUT + fancy indexing + sum, mesmo com 8× menos memory bandwidth. Memory bandwidth NÃO é o gargalo no nosso regime (resident set ~87 MB já cabe em L3). A vantagem do PQ vem de SIMD ints manuais (AVX2 VPSHUFB, etc) que pure numpy não tem. **Lição**: PQ é lever de compiled language com manual SIMD, não de Python idiomático com numpy. Revertido |
| Profile do worker (RINHA_PROFILE=1) em plateau #13 + IVF parameter sweep (nlist ∈ {2048, 4096, 8192}) | profile-only / sweep neutro | **Achados do profile**: internal total p99 = 1.25ms (mas k6 p99 = 47ms — gap 46ms é EXTERNO ao handler, HAProxy queueing). Stage breakdown: body_read p99 = 1.19ms (async I/O tail), faiss_search avg=350µs p99=577µs (não os 4ms que assumi), vectorize avg=28µs, decode=4µs, response=5µs. Boundary rate REAL = 10% (não 7.4% que assumi). **Sweep nlist=4096 nprobe=16**: score 3847 (+21 vs 3826, dentro do ruído), FP 38 FN 2 — det levemente melhor, p99 -2ms. nlist=8192: det piora (FP 43 FN 5) porque clusters menores spillam true K-NN entre vizinhos. Buffer pre-alloc + per-cluster matmul testado: 406µs (PIOR que baseline 350µs) — BLAS prefere matmul grande mesmo pagando concat alloc. `np.concatenate(out=)` também 375µs (slower). **Lição**: numpy `concatenate + np.dot` está near-óptimo pra este regime; tentativas de "otimizar alloc" perdem porque BLAS overhead per call domina |
| Análise dos 33 FP + 4 FN via test-data (FERIU REGRA) → reanalysis via REFERENCES only | **revertido por ética** | Originalmente analisamos `bench/test-data.json` e encontramos: 29 dos 33 FPs vêm de `fast_fraud` (amount>2996) e TODOS têm MCC ∈ {7801, 7802, 7995} (gambling). Adicionar MCC blocklist no fast_fraud reduziu E de 45→22 (sim det 2501→2591). **MAS**: derivar regra dos FPs no test-data **fere a regra oficial** ("Não é permitido usar os payloads do teste como referência"). Revertido. **Reanálise via references.json.gz only**: amount>2996 com MCCs 7801/7802/7995 tem fraud_rate **100%** nas refs (276k+275k+276k refs cada bucket). Ou seja, em refs a regra `amount>2996→fraud` está CORRETA para gambling. Os 29 FPs vêm de **divergência do test data** (queries gambling-high-amount que data generator marca legit, contradizendo as refs). Não há ajuste ético que feche esses 29. Plateau #13 confirmado como **teto legítimo** sob constraint Python idiomático + sem peeking no test data |
| HAProxy tunings: `http-reuse always`, `tune.maxaccept 32`, `tune.bufsize 8192` | 0 a -137 sim | UDS sockets já são cheap → http-reuse always é neutro/leve negativo (3 runs avg 3805 vs 3826). tune.maxaccept=32 neutro (avg 3834). tune.bufsize=8192 catastrófico (-137, 3689) — buffer 16KB→8KB quebra throughput em algum path interno. HAProxy não é o gargalo no real (0.20 CPU @ 22% util) — não há tuning não-comportamental que mova a agulha sem violar regra "LB só distribui requisições" |
| Numba `@njit(fastmath=True)` kernel pra IVF distance compute — prévia REAL #8165 (commit `a6eb3c7`) | sim +24, **real −29** | Add deps numba+llvmlite (~60MB), kernel custom com loop 14-dim unrolled manualmente → AVX2 FMA direto. **Crítico**: warmup com `np.zeros(14)` cai em fast-path homogeneous, NUNCA toca kernel. Tem que chamar `_knn_dists_numba` direto em __init__ com arrays mmap reais pra JIT cachear. Sem isso: 12 timeouts no início + IVF avg 1254µs. Com warmup correto: IVF avg 350→236µs (-33%), p99 577→355µs (-38%) em sim. 3 sim runs: 3848/3857/3844 = avg 3850 (+24). Real prévia: p99 21.1→22.5ms (+1.4), det idêntico, score 4148 (-29 vs plateau 4177). Sim CPU-bound (0.16) supervaloriza ganho de cycles; real (0.40) tem folga e o overhead numba (ABI per-call ~1µs, +25MB resident pressure) supera economia. Revertido. **Lição**: kernel compute speedups só valem em real se sim mostra **≥+100** ganho ou o real está claramente CPU-bound. Ganhos sim ≤50 dentro do range ±30 de variância de prévia significam que o lever provavelmente é noise no real |
| Granian flags sweep em cima de plateau #15 (uvloop+w2): `--backpressure 1`, `--http1-pipeline-flush`, `--backlog 256`, `--task-impl rust`, `--http 2`, `--runtime-mode mt --runtime-threads 2`, HAProxy `http-no-delay` | catastrófico a neutro | `--backpressure 1` = **−6000** (serializa requests → 38298 timeouts). `--task-impl rust` warning "not available on Python ≥ 3.12, falling back to asyncio" — só uvloop ficou ativo, sem benefício extra. `--http 2 + proto h2` em backend = HAProxy não conecta no UDS (Granian h2 over UDS sem TLS é zona obscura). `--backlog 256` = -6 sim (default 1024 OK). `--runtime-mode mt --runtime-threads 2` = 0 sim (GIL serializa Python). `--http1-pipeline-flush` = 0 sim isolado; mantido por hygiene. HAProxy `http-no-delay` = 0 sim (Nagle não estava ativo de qualquer jeito em UDS). HAProxy `nbthread 2` + workers=2 = 0 (revisitado, mesmo resultado de plateau pré-uvloop) |
| Pre-touch das mmap pages pequenas (`vec_norms.sum() + cluster_labels.sum() + labels.sum()`) em `__init__` | 0 sim (3883 vs 3890 baseline w2) | Forçar páginas residentes pra eliminar page-fault no primeiro burst. Memória subiu de 89→90 MB (vec_norms 12MB + cluster_labels 3MB + labels 3MB). **Skip vectors.npy** (168MB) — pre-touch full encherіa 178MB > 165 budget e OOM. Sim mostrou marginal/negativo (dentro do ruído ±40). **Diagnóstico**: page faults não são o gargalo. Profile mostra max p99 = 65ms mas isso é cgroup CPU throttle, não disk I/O. mmap mode lazy + MADV_WILLNEED já é o ótimo |
| nlist=4096 + nprobe=12 com workers=2 (já em uvloop+w2) | +2 (tied) | Cluster scan 4× menor (17k → 4.4k vectors), mas IVF avg interno só caiu 350→327µs (-7%, pequeno porque centroid scan dobrou). Detection trocou (FP 33→38, FN 4→2 → E 45→44 marginal). 3 sim runs avg 3892 vs w2 3890 = +2. Não compensa a divergência de detecção pro ratio. Não burnado. **Confirma**: nlist tuning saturada — qualquer mudança nlist/nprobe trades p99 ↔ det dentro do mesmo total score |
| HTTP/2 backend (`granian --http 2` + HAProxy `server ... proto h2`) | falha de conexão | HAProxy tenta speak h2c (HTTP/2 cleartext) sobre UDS pra Granian em mode `--http 2`. Granian default = h2 com TLS expectation. Resultado: "Server apis/api1 is DOWN, Connection refused". Não há combo Granian/HAProxy h2c sobre UDS documentado que funcione. Mantém HTTP/1 entre LB e backend |
| Health check `inter 10s` em vez de `1s` | +8 sim noise | Reduz background overhead (1 req/s/backend → 0.1 req/s/backend), avg subiu marginal 3890→3898 dentro do ruído. **Risco em real**: se worker travar, LB demora até 10s pra detectar e re-rotear → http_errors picam. Mantém 1s por segurança (1 RPS overhead é < 0.1% load) |
| 3 instâncias × 0.107 CPU sim cada com workers=2 | -130 sim | Após plateau #15 (workers=2 dentro de 2 containers), testou splitar em 3 containers. 6 worker processes totais mas cada com menos CPU absoluto. Memória teoricamente folgada (87 MB cada + mmap shared). Em sim 0.107 CPU/container × 2 workers = 0.053 CPU efetivo cada — muito apertado. Score caiu 3890→3760. **Sweet spot é workers=2 dentro de 2 containers**, não 3 containers. Container-level fork compartilha mmap; cross-container precisa mmap separado |
| Granian `--workers 3` e `--workers 4` por container | +9 (w3) e +8 (w4) sim, marginal | Workers=2 → 89 MB / 165, workers=3 → 105 MB / 165, workers=4 → 122 MB / 165. Sim sweet spot ainda é workers=2 — diminishing returns + memória apertando. Workers=4 (8 processos totais) chegou 75% memória, risco de OOM em burst. Não burnado (sim gain ≤ noise) |
| Numba kernel + uvloop + workers=2 (composição com plateau #15) | +5 sim (noise) | Após numba prévia real -29 isoladamente, testou se uvloop+workers=2 mudaria a dinâmica. Memória subiu 89→113 MB (+24 MB/processo × 2 processos no container = +48 MB total). 3 sim runs: 3898/3895/3892 avg 3895 vs w2 baseline 3890 = +5 dentro do ruído. Probabilidade de real ≥0 é baixa dado o histórico do numba em real. **Decisão**: skip preview, numba não compõe |
| nprobe=16 em cima de plateau #15 (uvloop+w2) | -20 sim | Tentativa de comprar +det com nprobe maior agora que I/O scheduling tá melhor. Detection +5 (FP 33→31), mas p99 +2.5ms (40.87→43.38). Score 3870 vs 3890 = -20. nprobe ainda saturado em 12 |
| HAProxy CPU split alternativo (0.145 API × 2 + 0.11 LB sim) | -46 sim | Tentou dar mais CPU pra LB ao custo dos workers. Workers ficaram CPU-starved → score 3844 vs 3890 = -46. **Confirma**: workers são o gargalo de CPU sob carga, não HAProxy (que está em ~22% util mesmo no real) |
| Decision tree pre-classifier "Mode A+B" (fksegundo-style) — sklearn DecisionTreeClassifier max_leaves=512 treinada nos 223k refs que escapam do 2-rule | -9 sim (-3 det, +1 FP) | Add sklearn como dep (+80MB build, runtime usa só numpy via flat arrays). Tree exportada como `tree.npz` com `(features, thresholds, left, right, leaf_purity, leaf_label, leaf_cluster_bitset)`. Mode A: leaf purity ≥ 0.999 → retorna label do leaf, skip IVF. **Treino só no residual** (refs que escapam de `amount/avg ≤ 0.971` OR `amount > 2996`) — focou tree no boundary onde poderia ajudar. Resultado: 3 leaves Mode A cobrindo 52.36% dos residual refs (todas com label=fraud). Numpy tree traversal ~5µs por query. **Falha**: sim 3881 vs 3890 = -9, FP 33→34 (+1, mesma assinatura do lookup table). Cell-purity em refs ≠ KNN-correct em test queries: o leaf vota "fraud" baseado nos refs do residual, mas alguma query test cai no leaf sendo na verdade legit (data divergence). Compute saving (350µs IVF skipped em 52% das boundary) não move ponteiro no real (mesma lição que numba: real CPU-folgado, compute saving não translada). Revertido sem prévia |
| Numba revisitado em cima de plateau #15 — prévia REAL #8301 (commit `f2065f2`) | sim -10, **real -3170 CATASTRÓFICO** | Re-aplicou o numba kernel sobre uvloop+workers=2. 6 sim runs avg 3880 vs w2 baseline 3890 (-10, dentro do ruído). IVF interno avg 415→265µs (-36%) confirmado. Memória só +4 MB (numba_cache provavelmente compartilhado entre processes via mmap). **Catástrofe em real**: score 4493→1323 (-3170), **1222 HTTP errors**, E=6154 (45 baseline). p99 16.83→30.41ms (+13.6ms). Detection_score colapsou para -193 (rate_component 943 + absolute_penalty -1136). **Hipótese**: race entre os 2 processos Granian do mesmo container ao escrever/ler `__pycache__/*.nbc` cache do numba simultaneamente no boot, ou crash de worker sob bursts de carga. Warmup no `__init__` chama o kernel mas só 1 vez por processo — se o 2º processo do mesmo container ainda não terminou de inicializar quando o k6 começa, requests pegam um worker durante JIT. **Lição final do numba**: numba e workers=2 são incompatíveis sem mechanism de sincronização extra (lock file, parent-process precompile). Add deps de 60MB pra ganhar nada em real e arriscar -3000 — descartar definitivamente. Revertido em `690c802` |

## Descartados pré-single-IVF (cada um testado, com motivo)

| Tentativa | Resultado | Onde quebrou |
|---|---:|---|
| `scipy.spatial.cKDTree` | drop | float64 interno → estoura RAM; incompatível com mmap |
| int16 quantization (numpy hot path) | -48ms p99 | `astype(int32)` por query aloca 28 MB no hot path |
| Partição mais fina (512 chaves, amount 3-bit) | -43 sim | topo do amount denso (clamp em 10000) → split só 26%; vizinhos se separam → recall cai mais que latência ganha |
| HNSW coarse quantizer no IVF (`IndexHNSWFlat`) | 0 | `IndexFlatL2` em 14-dim já é SIMD-vetorizado; coarse step não dominava |
| `IndexFlatL2` puro por partição (exato dentro) | -6000 sim | KNN exato em ~283k vetores médios não cabe no Haswell saturado, 93% timeout |
| Feature weights `\|ρ\|` por dim | -116 sim | métrica uniforme já 100% acurada na references → perturbação só piora |
| Padding 14→16 dim (SIMD alignment) | 0 sim | Faiss alinha internamente; tail handling não era gargalo |
| Tightness factor no bbox prune (< 1.0 ou > 1.0) | 0/-1000 sim | bbox natural já calibrado; tight<1.0 corta partições críticas, tight>1.0 só adiciona custo |
| Always-bbox compute + min-lb verified unanime-exit | -460 sim | recupera +360 detecção (36 wrong-unanimes) mas numpy/query sob saturação joga p99 de 100ms pra 560ms |
| Smart unanime-exit com `homogeneous_score` filter | -390 sim | mesmo problema: bbox-compute por query mata p99 |
| Neighbor list pré-computada (K=32 por partição) | -510 sim | fancy indexing `bbox_max[neighbors]` ainda custa caro; não baixa bbox-cost |
| 6-rule fast-path (km_home/installments/km_last/tx24h adicionais) | 0 sim | queries extras eram fáceis pro KNN; saving ~1µs/query, neutro |
| Decision tree depth=5 sklearn como fast-path | proj. neutro | 2 leaves ≥99.99% pureza cobrem 96.5% (vs 92.6% com 2 regras), adiciona ~36 erros — perfil parecido com 6-rule |
| `IndexIVFScalarQuantizer` int8 (nprobe=2 e nprobe=4) | -250 sim | quantização introduz erro de boundary irrecuperável; +probes não compensa |
| `IndexRefineFlat` sobre IVFSQ8 (int8 wide + float32 rerank, k_factor=4) | **-6000 sim** | refine vectors carregam em heap (não mmap), 178MB+ por worker estoura budget → OOM → 51k timeouts |
| Custom rerank em Python sobre `vectors.npy` mmap'd (IVFSQ8 wide + numpy rerank, k_factor=2/4) | -10 sim, +37MB | funciona (sem -6000 do RefineFlat porque mmap), detection +10 (rerank exato recupera int8). Mas p99 +5ms e index 84→221MB. Net negativo vs fp16. **Concept-proven mas pior que baseline.** |
| Per-cluster bbox via Faiss `invlists`/`search_preassigned` | descartado sem implementar | requer compute por query de bbox lb pra ≤1024 clusters em Python (~5µs/query). Mesmo padrão dos experimentos always-bbox/smart-exit/neighbor-list que perderam: trabalho Python per-query sob saturação no Haswell ampliar p99 mais do que pruning algorítmico salva. Marginal mesmo se ganhasse algo. |
| Granian `--backpressure 256 --http1-buffer-size 8192` | 0 sim | Concorrência real ~22 in-flight, backpressure 256 nunca aciona; buffer menor para responses de 50B não pressiona |
| `MAP_POPULATE` em todos os `.faiss` no startup (single syscall, alternativa limpa ao pre-touch byte-a-byte) | 0 sim | Os 2 min de ramp-up do k6 já aquecem page cache organicamente; pre-fault não muda tail |
| msgspec `gc=False` em todas as Structs + drop unused `FraudRequest.id` | 0 sim | gc=False é hint sem mudança semântica; struct decode já era ~3µs/req — saving sub-µs invisível |
| `B+C+D` empilhados | **-41 sim** (regrediu) | Stacking não compõe; run-to-run variance amplifica quando tudo é ruído individual. Não há sinal a tirar |
| `mlock` no índice via `ctypes.mlock` + `ulimits.memlock:-1` (~80 MB locked, 90/165 MB no cgroup) | 0 sim | Funciona técnicamente, mas sim sem competição por page cache não modela o cenário onde mlock paga. No real Haswell (8 GB RAM, sem outras workloads), também provavelmente neutro — page cache já não evita pages do índice |
| `FRAUD_THRESHOLD 0.6 → 0.4` (reclassifica score 0.4 = 2/5 vizinhos fraude → fraude) | **-725 sim** | FP saltou 63→373, FN caiu 17→14. Score 0.4 é majoritariamente real-legit (8% fraude); custo FP×1 (+310) >> ganho FN×3 (+9) |
| `K_NEIGHBORS 5 → 3` | **-810 sim** | Granularidade pior (scores 0, 0.33, 0.67, 1.0): FP+115, FN+85 simultâneo. Menos vizinhos = boundary mais ruidoso em ambas direções. Latência ganho mínimo (~1ms) |
| Grid search das 2 regras de fast-path (200 thresholds × 14 dims, piso pureza 99.99%) | 0 (sem alternativa melhor) | Pair atual já near-optimal: joint cov 92.57%/99.99% pureza. Best grid alternative `amount/avg≤0.5` + `amount>3061`: 92.29% (-0.28%). `hour≤5h→fraud` interessante mas pior joint. Lever saturado |
| K-means fast-path (replace 2 rules) | descartado sem testar | Forçaria `vectorize` (~3-5µs) ANTES da decisão pra 92.6% das queries, vs 0.5µs da 2-rule atual em raw payload. Como segunda camada após 2-rule só atinge 7.4% (boundary hard) onde K-means não bate KNN. Análise neutra/negativa antes de implementar |
| `IndexHNSWSQ` fp16 M=8 efSearch=16 por partição | **-422 sim** | Index 84→247 MB blowing budget (api-1 97% mem), p99 spikes 51→101ms entre runs. Detection regride -224 (FP+20, FN+12) por M=8 sparso. HNSW + 14-dim + 84 partições é regime errado pra HNSW; IVF vence |
| PCA 14→8 dims (project pré-search) | **-1216 sim** | Detection colapsa 2043→835, erros saltam 81→490. Features são todas discriminativas (sem redundância); 14→8 joga signal fora. Index cai 84→57 MB e p99 fica igual, mas detection é catastrofica. Confirma que 14-dim é mínimo |
| `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 FAISS_NUM_THREADS=1` | 0 sim | faiss/numpy não estavam multithreading em ops de 14-dim de qualquer jeito |
| `LD_PRELOAD=libmimalloc.so.3` | -9 sim (ruido) | Alloc patterns em Python são dominados por interior structures (objects, dicts); allocator swap não move |
| `madvise(MADV_RANDOM)` em faiss mmap | inviável | Faiss owns o mmap e não expõe pointer pra advise externo; precisaria patch no Faiss |
| Recompilar faiss-cpu com `-march=haswell` | desnecessário | Wheel oficial `faiss-cpu 1.14.2` já contém `OPTIMIZE DD AVX2` (Dispatcher Dinâmico), runtime já usa AVX2 path em Haswell |
| HAProxy `maxconn 64` por backend | 0 sim | Concorrência real ~22 in-flight, threshold 64 nunca aciona, sem 503s, sem efeito no tail |
| `MAP_HUGETLB` explícito (2MB pages) | inviável | Host (e provavelmente runner) sem `nr_hugepages` reservados; pedido falha com ENOMEM. THP via `madvise(MADV_HUGEPAGE)` já aplicado |
| `faiss.omp_set_num_threads(1)` no init | 0 sim | Mesmo efeito de `OMP_NUM_THREADS=1`: faiss/numpy não multithreading em 14-dim |
| `partition_key` direto do payload raw (pulando vectorize na boundary) | descartado análise | Homogeneous partitions são extremos (todos labels iguais); boundary queries são por definição ambíguas → raramente caem em homogeneous. Adiciona ~2µs em 79% do boundary, economiza ~3µs em 21% → net negativo |
| Granian `--runtime-mode mt --runtime-threads 2` | 0 sim | Profile revelou que gap k6 vs interno (50ms) é **CPU throttle do cgroup**, não serialização do event loop. MT compartilha mesma quota → não move |
| Granian `--workers 2` por container (4 total) | 0 sim, memória 90% | Mesma quota CPU compartilhada → sem ganho. Memória aperta (150/165 MB) e ainda é risco de OOM |
| `MAX_EXTRA_PARTITIONS 8 → 4` | -261 sim | Profile mostrou faiss_search interno caiu 30% (1093→768µs p99), MAS k6 p99 IGUAL (51ms). **Comprova: gargalo é CPU throttle externo, não trabalho do handler.** Detection colapsou (FP+23, FN+15) pq cortar bbox cap mata recall |
| Granian ASGI raw (sem Starlette) | -34 sim | RSGI nativo é mais leve que ASGI mesmo dentro do Granian (não foi só dropar Starlette) — protocolo mais compacto, menos alloc por request |
| Uvicorn ASGI raw (sem Starlette) | -32 sim | ≈ Granian ASGI raw. Não bate Granian RSGI; Rust core do Granian + protocolo RSGI compactam mais |
| Hypercorn ASGI raw (sem Starlette) | -192 sim | Server Python-puro lento; p99 80ms vs 51ms do Granian RSGI |

## Findings algorítmicos da evolução

Findings que documentam decisões já tomadas. Pra regras vivas (gotchas, restrições de
infra, padrões de medição), ver `CLAUDE.md`.

- **Faiss IVFFlat + mmap** (`read_index(IO_FLAG_MMAP)`): ~0.3 ms/query, page cache
  compartilhado entre os 2 workers. Os vetores moram no índice (não guardar `.npy` separado).
- **scipy cKDTree** converte para float64 interno (estoura RAM) e é incompatível com mmap.
  Descartado.
- **int16** exige `astype(int32)` por query → aloca no hot path → lento. float32 + BLAS vence.
- **nprobe muda de ótimo conforme o resto do search evolui** (baked em `build_index.py`).
  Sob nprobe-only (sem bbox): nprobe=1 venceu (+790 vs nprobe=8: 738→1528) — cada lista IVF
  extra vira latência direta sob saturação. Depois do bbox-prune + unanimous-exit os fáceis
  saem rápido, então sobra orçamento pra recall maior nos boundaries: nprobe=2 venceu
  nprobe=1 (2921 vs 2512). nprobe=4 já piora (latência custa mais que a detecção rende).
  Após single-IVF (plateau 10+): nprobe=12 é o ótimo.
- **Asymmetric nprobe entre primary e extras** (era arquitetura per-partition, pré single-IVF):
  a primária paga a vasta maioria das queries via unanimous-exit, então custo dela domina o p99
  (mantém nprobe=2). Os extras só rodam em queries ambíguas após filtro do bbox — vale a pena
  cavar mais fundo neles. `EXTRAS_NPROBE=3` é o ótimo (+124 real: 2921→3045). Subir a 4 ou 5
  destrói o p99 sem ganho. Tradeoff dominante é recall-vs-latência nos extras.
- **Levers estruturais que falharam** (pré single-IVF):
  - Partição mais fina (512 chaves, amount em 3 bits): topo do amount é denso pelo clamp em
    10000 → split de só 26% no max, mas vizinhos se separam entre partições → recall caiu
    mais que a latência ganhou. **Regressão.**
  - Quantizador grosso HNSW (`IndexHNSWFlat` no IVF): `IndexFlatL2` em 14-dim já é SIMD,
    o passo grosso não dominava. ~30 µs vs ~30 µs. **Sem ganho.**
  - `IndexFlatL2` puro por partição (KNN exato dentro): satura o Haswell catastroficamente,
    93% timeout, **-6000**.
  - Feature weights por dim usando `|ρ|` (correlação com label): com métrica uniforme já
    100% acurada, qualquer perturbação só piora os k-vizinhos. **Regressão (-116).**
- **KNN k=5 EXATO dá 100% de accuracy sobre as references** (verificado em 2000 queries do
  test-data via brute-force BLAS, 0 TP/TN/FP/FN). Não tem regra escondida, não tem teto do
  KNN: o teto era a APROXIMAÇÃO do IVF. A combinação nprobe=2 + bbox cross-partition +
  unanimous-exit recupera quase todo o recall perdido (1.77% → 0.20% failure).
- **Bounding-box pruning cross-partition** (era em `search.py` pré-single-IVF; removido em
  plateau 10): salva `min`/`max` por dim por partição em `meta.json`; em runtime, depois da
  primária, calcula `lb_sq = ||q − bbox||²` vetorizado e visita só partições com
  `lb_sq < worst-of-top-K`, com merge incremental dos top-K e cap `MAX_EXTRA_PARTITIONS=8`.
  Sozinho custa ~127 pts de latência por query (`np.maximum`/`einsum`/`argsort`) → emparelhado
  com **early-exit por voto unânime** na primária para queries de alta confiança pularem o
  sweep. Combo deu **+815 no real** (1697 → 2512), detecção 560 → 1383, p99 quase inalterado.
- **Verificar unanime via bbox global tem teto mas custa caro demais** (pré-single-IVF).
  Sempre computar bbox (mesmo nas unanimes) recupera ~36 wrong-unanime-exits — detecção sobe
  ~360. Mas o numpy per-query sob saturação no Haswell joga p99 de 100ms pra 560-735ms (mesmo
  com neighbor list pré-computada por partição). Net negativo. A imprecisão das unanimes é o
  preço a pagar.
- **Fast-path determinístico antes do KNN** (2 thresholds hand-derivados em `rsgi_app.py`):
  `amount/avg ≤ 0.971` → legit; `amount > 2996` → fraud. Cobrem ~92.6% das queries com 99.99%
  accuracy em references. Pula vectorize + partition_key + Faiss inteiro pra essas queries —
  só ~7.4% boundary cai no KNN. Custou +188 real (3045→3233) cortando p99 de 100ms→65ms,
  **sem perder detecção** (mesmos 2043).
- **Mais regras de fast-path têm retorno marginal** (testado em sim). Adicionar 4 regras
  fraude extras (km_home, installments, km_last, tx24h thresholds) leva cobertura pra 96.5%
  com mesma pureza, mas a fatia extra é facil pro KNN também — só economiza ~1.2µs avg por
  query no agregado. Sim mostrou neutro. Decision tree depth=5 (sklearn offline) acharia
  rules similares — projetado igual.
- **`IndexIVFScalarQuantizer` int8 perde recall irrecuperável**. Testado em sim: nprobe=2
  caiu de 3232→2969 (-263), nprobe=4 não compensou (2983). A quantização introduz erro de
  boundary que mais probes não recupera. Index size 147→53MB seria bom mas detection cara
  demais. Stay fp32 IVFFlat (plateau 12 confirmou fp16 também perde — em 14-dim qualquer SQ
  decompression custa mais do que a economia de memória).
- **fp16 vs fp32 em 14-dim** (descoberto no diagnose pre-plateau-12): `IndexIVFScalarQuantizer`
  fp16 leva 798µs/query bare-metal vs `IndexIVFFlat` fp32 102µs (7.8× mais lento). Em alta
  dimensionalidade (100+) a decompressão se amortiza sobre mais SIMD work, mas em 14-dim ela
  domina. Memória 84→184MB foi compensada por mmap evict de cold pages (resident set ficou
  estável em ~87MB no sim).
- **Sim throttled prediz real só dentro do mesmo motor**. Mudanças de parâmetro (niter, nprobe,
  CPU split, fp16↔fp32 dentro de Faiss) traduzem bem. Mudanças de substrato (Faiss → numpy
  custom) divergem: numpy IVF híbrido mostrou +34 sim e −74 real porque sim throttle (0.16 CPU)
  amplifica Python overhead diferentemente do real Haswell (0.40 CPU). Bench bare-metal
  (`taskset -c 0`) costuma estar mais alinhada ao real que sim throttled pra essas escolhas.
